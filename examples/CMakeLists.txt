# Add the Sandbox example
add_subdirectory(Sandbox)

# Add the UnitsExample
add_subdirectory(UnitsExample)

# Add the CryptoCapabilities example
add_subdirectory(CryptoCapabilities)

# Add the CryptoCookbook example
add_subdirectory(CryptoCookbook)
